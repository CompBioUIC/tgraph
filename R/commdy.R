#
#
#

# take a gtm object and feed it into the commdy c program
# figure out how to call that from R


# two routes:
# 1) just use executables
# 2) have parameter input/flexibility, use that to edit the makefile,
#    recompile the executable and run that
